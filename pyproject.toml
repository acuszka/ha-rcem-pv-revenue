[project]
name = "ha-rcem-pv-revenue"
version = "0.1.7"
description = "Home Assistant custom integration for Polish PSE RCEm PV export revenue."
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
test = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
